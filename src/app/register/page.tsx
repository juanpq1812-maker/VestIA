import PaginaStub from "@/components/ui/PaginaStub";

export default function RegisterPage() {
  return (
    <PaginaStub
      titulo="Registro"
      descripcion="Aqui ira el formulario para crear una cuenta nueva (Supabase Auth)."
    />
  );
}
