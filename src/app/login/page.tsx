import PaginaStub from "@/components/ui/PaginaStub";

export default function LoginPage() {
  return (
    <PaginaStub
      titulo="Login"
      descripcion="Aqui ira el formulario para iniciar sesion (Supabase Auth)."
    />
  );
}
